# App lives in the pomodoro-frontend/ subdirectory, so point Netlify there.
[build]
  base = "pomodoro-frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for client-side routes (React Router).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
